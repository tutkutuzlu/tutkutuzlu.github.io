# Tutku Tuzlu Projects

Static, data-driven project portal for [tutkutuzlu.github.io](https://tutkutuzlu.github.io/).

## Commands

```bash
npm run check
npm run dev
```

Projects are defined in `data/projects.json`. Adding a published project requires one metadata record; the build generates the project card and structured data.
