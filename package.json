{
  "name": "tutku-tuzlu-projects-portal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "node --test && node scripts/build.mjs",
    "dev": "node scripts/dev.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
